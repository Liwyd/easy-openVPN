[project]
name = "eovpanel-installer"
version = "0.2.0"
description = "eovpanel CLI — clean command-line installer and configurator"
requires-python = ">=3.12"
license = "MIT"
dependencies = []

[project.scripts]
eovpanel = "installer.cli:main"

[tool.ruff]
target-version = "py312"
line-length = 120
